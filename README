Ce code utilise codeigniter comme base avec toute une liste des fonctions pour étendre le comportement et facilité les choses

Pour installer il suffit de copier le contenu, modifier le fichier application/condif/database et c'est fini